import React from 'react';
import { SafeAreaView } from 'react-native';
import Account from './screens/Account';

const App = () => {
  return (
    <SafeAreaView>
      <Account />
    </SafeAreaView>
  );
};

export default App;
